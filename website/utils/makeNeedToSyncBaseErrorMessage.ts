export const makeNeedToSyncBaseErrorMessage = (firstPart: string) => {
    return `${firstPart} The owner of this extension needs to open the miniExtensions Airtable marketplace app to sync their base.`;
};
